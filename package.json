{
  "name": "crawler",
  "version": "1.0.0",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cheerio": "^1.0.0-rc.3",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mysql": "^2.17.1",
    "request": "^2.88.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.1"
  },
  "scripts": {
    "start": "node index.js",
    "heroku-postbuild": "npm install --only=dev && npm install && cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": "12.15.0",
    "npm": "6.14.2"
  }
}
